#pragma once

/*#include "Engine.h"
#include "GameEngineBase.h"
#include "abstractMeteor.h"
#include "Bouton.h"

class MyGameEngine : public GameEngineBase {
private:
    std::vector<Shot*>* tire;
	std::vector<movingEntities*> *ennemy;
	std::vector<Bouton*> *listener;
	boolean moving;
public:
	MyGameEngine(std::vector<Shot*>* tire_, std::vector<movingEntities*> *ennemy_, std::vector<Bouton*> *listener_)
		:tire(tire_), ennemy(ennemy_), listener(listener_), moving(false)
	{}

    virtual void idle();

	void isHit(int s);
	void fired(int pos);
	void playOrStop();
	boolean collision(float x1, float y1, float x2, float y2);
};*/


